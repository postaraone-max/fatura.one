/**
 * FUTURE: Real login/session service
 *
 * This file will contain the real functions for:
 *  - reading the user session
 *  - checking if the user is authenticated
 *  - mapping session data to CurrentUser
 *
 * For now, this is ONLY a placeholder.
 * No logic is active except a null-returning function below.
 */

export async function getSessionUser() {
  // FUTURE: replace with real session reading
  return null;
}
