/**
 * @deprecated
 * Test helper removed.
 *
 * This file intentionally throws if referenced to prevent
 * accidental use in production.
 */

export async function getTestPlanStatus(): Promise<never> {
  throw new Error("TEST_PLAN_HELPER_REMOVED");
}
